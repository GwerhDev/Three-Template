{
  "dependencies": {
    "fflate": "^0.8.0",
    "gh-pages": "^5.0.0",
    "three": "^0.152.2"
  },
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^2.6.4"
  }
}
